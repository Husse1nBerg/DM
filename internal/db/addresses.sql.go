// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: addresses.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createAddress = `-- name: CreateAddress :one
INSERT INTO addresses (
        street,
        city,
        state,
        postal_code,
        country,
        latitude,
        longitude
    )
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, street, city, state, postal_code, country, latitude, longitude, created_at, updated_at, deleted_at
`

type CreateAddressParams struct {
	Street     *string
	City       *string
	State      *string
	PostalCode *string
	Country    *string
	Latitude   pgtype.Numeric
	Longitude  pgtype.Numeric
}

func (q *Queries) CreateAddress(ctx context.Context, arg CreateAddressParams) (Address, error) {
	row := q.db.QueryRow(ctx, createAddress,
		arg.Street,
		arg.City,
		arg.State,
		arg.PostalCode,
		arg.Country,
		arg.Latitude,
		arg.Longitude,
	)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.Street,
		&i.City,
		&i.State,
		&i.PostalCode,
		&i.Country,
		&i.Latitude,
		&i.Longitude,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const getAddressByID = `-- name: GetAddressByID :one
SELECT id, street, city, state, postal_code, country, latitude, longitude, created_at, updated_at, deleted_at
FROM addresses
WHERE id = $1
`

func (q *Queries) GetAddressByID(ctx context.Context, id uuid.UUID) (Address, error) {
	row := q.db.QueryRow(ctx, getAddressByID, id)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.Street,
		&i.City,
		&i.State,
		&i.PostalCode,
		&i.Country,
		&i.Latitude,
		&i.Longitude,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const updateAddress = `-- name: UpdateAddress :one
UPDATE addresses
SET street = $2,
    city = $3,
    state = $4,
    postal_code = $5,
    country = $6,
    latitude = $7,
    longitude = $8,
    updated_at = CURRENT_TIMESTAMP
WHERE id = $1
RETURNING id, street, city, state, postal_code, country, latitude, longitude, created_at, updated_at, deleted_at
`

type UpdateAddressParams struct {
	ID         uuid.UUID
	Street     *string
	City       *string
	State      *string
	PostalCode *string
	Country    *string
	Latitude   pgtype.Numeric
	Longitude  pgtype.Numeric
}

func (q *Queries) UpdateAddress(ctx context.Context, arg UpdateAddressParams) (Address, error) {
	row := q.db.QueryRow(ctx, updateAddress,
		arg.ID,
		arg.Street,
		arg.City,
		arg.State,
		arg.PostalCode,
		arg.Country,
		arg.Latitude,
		arg.Longitude,
	)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.Street,
		&i.City,
		&i.State,
		&i.PostalCode,
		&i.Country,
		&i.Latitude,
		&i.Longitude,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}
